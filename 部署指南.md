# 伴学应用部署到 learn.xiaoyuermama.top 操作指南

## 前置条件

- GitHub 账号（你已有：83024633-eng）
- Vercel 账号（你已有，关联了 GitHub）
- 域名 xiaoyuermama.top，DNS 在 DNSPod 管理
- 本地已安装 git（如未安装：https://git-scm.com/downloads）

---

## 第 1 步：在 GitHub 创建新仓库

1. 打开 https://github.com/new
2. Repository name 填：`english-learning-companion`
3. 选择 **Public**（Vercel Hobby 免费版要求公开仓库）
4. **不要**勾选 "Add a README file"（我们已有）
5. 点击 **Create repository**

---

## 第 2 步：上传文件到 GitHub

### 方式 A：网页上传（最简单）

1. 在刚创建的仓库页面，点击 **uploading an existing file**
2. 把 `github-deploy/` 目录里的所有文件拖进去：
   - index.html
   - sw.js
   - manifest.json
   - qr-code.svg
   - vercel.json
   - .gitignore
   - README.md
3. Commit message 填：`v39 初始版本`
4. 点击 **Commit changes**

### 方式 B：命令行上传（以后更新用）

```bash
# 进入 github-deploy 目录
cd /Users/yhm/WorkBuddy/2026-07-14-16-57-32/github-deploy

# 初始化 git 仓库
git init
git add .
git commit -m "v39 初始版本"

# 关联远程仓库并推送
git remote add origin https://github.com/83024633-eng/english-learning-companion.git
git branch -M main
git push -u origin main
```

---

## 第 3 步：在 Vercel 部署

1. 打开 https://vercel.com/dashboard
2. 点击 **Add New...** → **Project**
3. 在 "Import Git Repository" 中找到 `english-learning-companion`
4. 点击 **Import**
5. 配置：
   - Framework Preset：**Other**（或保持默认）
   - Root Directory：`./`（默认）
   - 不需要改任何其他设置
6. 点击 **Deploy**
7. 等待约 1 分钟，部署完成
8. 此时会有一个临时地址：`english-learning-companion-xxx.vercel.app`
   可以先点开看看是否正常

---

## 第 4 步：绑定子域名 learn.xiaoyuermama.top

### 4.1 在 Vercel 添加域名

1. 进入刚部署的项目 → **Settings** → **Domains**
2. 输入：`learn.xiaoyuermama.top`
3. 点击 **Add**
4. Vercel 会显示需要添加的 DNS 记录，通常是：
   - 类型：CNAME
   - Name/Host：learn
   - Value：cname.vercel-dns.com

### 4.2 在 DNSPod 添加 DNS 记录

1. 打开 https://console.dnspod.cn/dns/list
2. 找到 `xiaoyuermama.top` → 点击进入
3. 点击 **添加记录**
4. 填写：
   - 主机记录：`learn`
   - 记录类型：`CNAME`
   - 记录值：`cname.vercel-dns.com`
   - TTL：默认（600）
5. 点击 **保存**

### 4.3 等待生效

- 回到 Vercel Domains 页面，点 **Refresh**
- 通常 1-5 分钟内 Vercel 会自动验证
- 验证成功后，Vercel 自动申请 SSL 证书
- 看到 ✅ 就说明完成了

---

## 第 5 步：验证

打开浏览器访问：**https://learn.xiaoyuermama.top**

应该能看到伴学应用首页 🎉

---

## 以后如何更新应用

每次修改了 `index.html`，只需要：

```bash
cd /Users/yhm/WorkBuddy/2026-07-14-16-57-32/github-deploy

# 从 deploy 目录复制最新文件
cp ../deploy/index.html .
cp ../deploy/sw.js .

# 更新版本号（如果改了 CACHE_NAME）
# 编辑 sw.js 里的 CACHE_NAME

# 推送到 GitHub
git add .
git commit -m "v40 更新说明"
git push
```

Vercel 会自动检测到推送，1 分钟内完成部署。

---

## 注意事项

1. **不要把 `.env` 或含密钥的文件推到 GitHub**
2. **每次更新 sw.js 里的 CACHE_NAME**（如 v39 → v40），否则用户的浏览器不会更新缓存
3. **PWA 添加到主屏幕**：在手机浏览器打开 `https://learn.xiaoyuermama.top`，选择"添加到主屏幕"，就会像 App 一样显示图标
4. **数据隔离**：学习记录存在浏览器的 localStorage 中，绑定到 `learn.xiaoyuermama.top` 这个域名，不会丢失
